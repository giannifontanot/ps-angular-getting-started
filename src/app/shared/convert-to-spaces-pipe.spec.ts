import { ConvertToSpacesPipe } from './convert-to-spaces-pipe';

describe('ConvertToSpacesPipe', () => {
  it('should create an instance', () => {
    expect(new ConvertToSpacesPipe()).toBeTruthy();
  });
});
